/**
 * main.js
 * 
 * The main view is where most of the app's components are present.
 * All the logic related to the main view core components is here.
 */

var logger = Logger.getInstance();

// Make draggable
Dragger(document.getElementById("open-tools-draggable"));